[package]
name = "claims-contract"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = "20.0"

[profile.release]
opt-level = "z"
overflow-checks = true
lto = true
codegen-units = 1
strip = true
